import { AmountRangePipe } from './amount-range.pipe';

describe('AmountRangePipe', () => {
  it('create an instance', () => {
    const pipe = new AmountRangePipe();
    expect(pipe).toBeTruthy();
  });
});
