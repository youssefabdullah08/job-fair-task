import { FillterbynamePipe } from './fillterbyname.pipe';

describe('FillterbynamePipe', () => {
  it('create an instance', () => {
    const pipe = new FillterbynamePipe();
    expect(pipe).toBeTruthy();
  });
});
